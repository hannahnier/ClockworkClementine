{
  "name": "clockworkclementine",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startLocally": "npm install && cd frontend && npm install && cd ../backend && npm install && cd .. && npm-run-all --parallel start:backend start:frontend",
    "start:backend": "cd backend && nodemon server.js",
    "start:frontend": "cd frontend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
